<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="LightTheme" parent="AppTheme">
        <item name="textColor">#000000</item>
        <item name="buttonColor">#BB86FC</item>
        <item name="bgColor">@color/bgColorLight</item>
        <item name="elementColor">#FFF</item>
    </style>

    <style name="DarkTheme" parent="AppTheme">
        <item name="textColor">#e6f5ff</item>
        <item name="buttonColor">#834c69</item>
        <item name="bgColor">@color/bgColorDark</item>
        <item name="elementColor">#3c4f65</item>
    </style>

</resources>
